#include "Modules/ModuleManager.h"

// ClearanceSim is a code-only runtime module with no special startup/shutdown
// needs, so the default module implementation is sufficient.
IMPLEMENT_MODULE(FDefaultModuleImpl, ClearanceSim);
